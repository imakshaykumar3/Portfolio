Add these files here before deploying:

- resume.pdf      → your actual resume PDF (linked from the navbar "Resume" button)
- og-image.png    → 1200x630 image used for social share previews (LinkedIn/Twitter link unfurls)
- favicon.ico     → browser tab icon

None of these are included since they're personal binary assets — drop them straight
into this /public folder with these exact filenames and the existing links in
Navbar.tsx and app/layout.tsx will pick them up automatically.
